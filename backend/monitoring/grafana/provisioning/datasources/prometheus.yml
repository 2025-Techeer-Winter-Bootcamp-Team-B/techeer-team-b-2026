# ============================================================
# 📊 Grafana 데이터소스 프로비저닝
# ============================================================
# Prometheus를 데이터소스로 자동 설정
# ============================================================

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: Prometheus
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
      httpMethod: POST
      queryTimeout: "60s"